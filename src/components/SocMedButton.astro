---
interface Props {
    link: string;
    icon: string;
}
const { link = "https://example.com", icon = "fa fa-question" } = Astro.props;
---
<a class="socmedButton" href={link} target="_blank">
    <i class={icon}></i>
</a>